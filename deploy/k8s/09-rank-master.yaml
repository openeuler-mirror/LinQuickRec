# RankMaster 精排主图服务
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rank-master-service
  namespace: linquickrec
  labels:
    app: rank-master-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rank-master-service
  template:
    metadata:
      labels:
        app: rank-master-service
    spec:
      containers:
      - name: rank-master
        image: 192.168.84.245:5000/linquickrec/rank-master:latest
        ports:
        - containerPort: 8004
          name: rpc
        envFrom:
        - configMapRef:
            name: linquickrec-config
        env:
        - name: SERVER_PORT
          value: "8004"
        resources:
          requests:
            cpu: "1"
            memory: "2Gi"
          limits:
            cpu: "2"
            memory: "4Gi"
        readinessProbe:
          tcpSocket:
            port: 8004
          initialDelaySeconds: 10
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: 8004
          initialDelaySeconds: 15
          periodSeconds: 15
        volumeMounts:
        - name: log
          mountPath: /var/log/linquickrec
      volumes:
      - name: log
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: rank-master-service
  namespace: linquickrec
spec:
  selector:
    app: rank-master-service
  ports:
  - name: rpc
    port: 8004
    targetPort: 8004
  type: ClusterIP
